To do:
~ Make buttons-column centered
~ Fix missing membership fees when startDate is null
~ Fix order of starting and ending balances
~ Remove $ from before distributions in report
- Adjust spacing in receipt
X Fix receipt still loading when done
	X Add parameter to get to not json the response, or make a new method
X Parenthesize savings value if negative (member home)
X Confirm what happens when a member is changed from church to individual or vice versa (update distributions_applied)
X Fix deleting abnormality for payments (doesn't do little spinner)
X Change sbyte for payment percentage to short
X An to a on payment edit page
X Less spacing in results pages
X Smaller font in details pages
X Enable churches as members:
	- Boolean field
	- Don't give them dividends or interest
	- Sort by isChurch
- Nicer small receipt printout (A3) - half of A4 for
	X transactions and
	- payments
X Add useful SACCO-wide stats to dashboard
	- Total shares (and value)
	- Total savings
	- Total balance
	- Total outstanding loan balance
X Reports:
	- Member account statement (incl transactions, dividends, fees)

Next version:
- Change viewer role to basic and add new "viewer" role - can also view quotas, payments, balances, but not edit anything.
- Censuses:
	- Add cong, parish, arch, and dioc details based on christian counts

To release:

X Fix shares and savings sorting together on member results page

To confirm in production:

To update in SQL:
	- Add is_church BIT field (default FALSE) to sacco_member table
	- All sprocs

	- Add Basic user role to user table (LATER)

Less important:
- Easier member selection (by name)
- Speed up report generation
	- Increase max user connections to 100 (waiting for Yosia)
- Enable easy backups
- Create better error page (add back useExceptionHandler('Error'))
- Add server-side unique check for dividend date
- Add name of logged-in user

Before leaving:
- Switch payment to SRD credit card (Richard)

Before November:
- switch instance to t3.nano (after free tier expires, around Nov. 2022)