To do:
- Move datestring to new helper and use datestring in nonunique distribution error
X Remove "Loan" column from payment receipt
X Add late fine to payment receipt
X Make churches get distributions
X Reorganize receipts to one column
X Report for all members (starting, ending balances and difference)
X Add comma to total number of shares on sacco dashboard
X Change viewer role to basic and add new "viewer" role - can also view quotas, payments, balances, but not edit anything.
X Censuses:
	- Add cong, parish, arch, and dioc details based on christian counts
X Add server-side unique check for dividend date


To update in SQL:
	- Add Basic user role to user table
	- All views (esp details)
	- All sprocs

To release:

To confirm in production:

Less important:
- Easier member selection (by name)
- Add name of logged-in user
- Require start date to be less then end date in search/reports

Much less important:
- Enable easy backups
- Create better error page (add back useExceptionHandler('Error'))

Before leaving:
- Switch payment to SRD credit card (Richard)

Before November:
- switch instance to t3.nano (after free tier expires, around Nov. 2022)